/* eslint-disable arrow-body-style */
/* eslint-disable no-unused-expressions */
const { expect } = require('chai');

describe('test /name/ahmed', () => {
  it('test route name', async () => {
    expect('x')
      .to
      .be
      .equal('x');
  });
});
